class Repository {
  constructor(id, title, language, description) {
    this.id = id;
    this.title = title;
    this.language = language;
    this.description = description;
  }
}

export default Repository;